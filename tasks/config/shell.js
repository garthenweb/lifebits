'use strict';

var path = require('path');

module.exports = function(grunt) {
  // var mongoDataPath = path.join(process.cwd(), grunt.config('pkg.paths.tmp'), grunt.config('pkg.paths.database'));
  // grunt.file.mkdir(mongoDataPath);

  // grunt.config.set('shell', {
  //   mongodb: {
  //     command: 'mongod --dbpath ' + mongoDataPath,
  //     options: {
  //       async: true,
  //       stdout: true,
  //       stderr: true,
  //       failOnError: true,
  //       execOptions: {
  //         cwd: '.'
  //       }
  //     }
  //   }
  // });
};
